[build]
  command = "npm run build"
  publish = "dist/donation-qr-netlify/browser"
  functions = "netlify/functions"

# SPA fallback so Angular routes work on refresh
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
